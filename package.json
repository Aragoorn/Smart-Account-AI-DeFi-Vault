{
  "name": "base-omni-vault-ai",
  "version": "1.0.0",
  "description": "Enterprise-grade autonomous smart account vault for Base",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:base-sepolia": "npx hardhat run scripts/deploy.ts --network base-sepolia",
    "deploy:base": "npx hardhat run scripts/deploy.ts --network base",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0",
    "hardhat": "^2.22.0"
  }
}